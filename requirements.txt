# No changes required — current requirements already cover exampleApp.py:
# - streamlit
# - plotly
# - numpy
# - matplotlib
# - shapely
# - pymongo
# Other entries (Flask, gunicorn) are optional and may be kept or removed per deployment needs.